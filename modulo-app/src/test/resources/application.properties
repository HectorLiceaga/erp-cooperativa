# Configuración de la Base de Datos (SOLO PARA TESTS)
# Usamos H2, una base de datos en memoria (mem:testdb)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Configuración de JPA para H2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Le decimos a Hibernate que cree las tablas al iniciar el test
# y las borre al finalizar.
spring.jpa.hibernate.ddl-auto=create-drop
